# AI Workflow Rules

## Guardrails
- Weights/checkpoints/adapters are NOT committed (git-ignored).
- Kaggle dataset + notebooks live here; keep metadata in sync.
- Scope to this repo; push to `Zolai-AI/zolai-training`.
