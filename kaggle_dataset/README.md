# Kaggle training bundle

Build scripts + dataset metadata only. The large `.jsonl` corpora are **not**
committed — mirror them from Kaggle / HuggingFace Hub during build (see
`zolai-datasets`). Training data never lives in git.
